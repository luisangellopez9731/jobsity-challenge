# Jobsity Challenge.

## How to run it.

First you need to have nodejs installed, you can read the documentation [in here](https://nodejs.org/).

After that you can run the next command to install the dependecies:

```
npm install
```

then you can run the project with the next command:

```
npm run start
```
